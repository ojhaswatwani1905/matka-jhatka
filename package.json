{
  "name": "matka-jhatka-app",
  "version": "1.0.0",
  "private": true,
  "description": "PlayArena - Premium Provably Fair Gaming & Matka Platform",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server",
    "postinstall": "npm run db:generate --prefix server",
    "build:client": "npm run build --prefix client",
    "build:server": "npm run build --prefix server",
    "build": "npm run db:generate --prefix server && npm run build:client && npm run build:server",
    "start": "npm run start --prefix server",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev": "npm run dev:server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
